[project]
name = "job-submit-status-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.2.0",
    "langgraph-checkpoint-postgres>=2.0.0",
    "psycopg[binary]>=3.1",
    "mcp<2",
    "httpx>=0.27",
    "databricks-sdk>=0.30",
    "mlflow>=2.16",
    "databricks-langchain>=0.20.0",
    "langchain-core>=1.6.3",
]

[dependency-groups]
dev = ["pytest>=8.0"]
